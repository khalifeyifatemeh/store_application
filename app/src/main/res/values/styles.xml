<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat" >
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme_ActionBar" parent="@style/Theme.AppCompat">
        <item name="icon">@drawable/ic_action_basket</item>
        <item name="android:keyIcon">@drawable/ic_action_search1</item>
    </style>
    <style name="MyTheme_menuitem" parent="@style/Theme.AppCompat">
        <item name="icon">@drawable/ic_action_basket</item>
        <item name="android:keyIcon">@drawable/ic_action_search1</item>
    </style>




</resources>
